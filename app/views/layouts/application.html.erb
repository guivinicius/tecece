<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= content_for?(:title) ? yield(:title) : "Tecece" %></title>
    <%= csrf_meta_tags %>

    <!-- Le HTML5 shim, for IE6-8 support of HTML elements -->
    <!--[if lt IE 9]>
      <script src="http://html5shim.googlecode.com/svn/trunk/html5.js" type="text/javascript"></script>
    <![endif]-->

    <%= stylesheet_link_tag "application" %>
  </head>
  <body style="padding-top:60px; background-color:#2A313A;">
    
    <div class="container well" style="min-height: 500px;">
      <div class="row">
        <div class="span12">
          <%= link_to "Adicionar Trabalho", new_project_path, :class => "btn btn-primary btn-small" %>

          <div class="btn-group pull-right">
            <a class="btn btn-primary" href="#"><i class="icon-user icon-white"></i> Minha conta</a>
            <a class="btn btn-primary dropdown-toggle" data-toggle="dropdown" href="#"><span class="caret"></span></a>
            <ul class="dropdown-menu">
              <li><a href="<%= edit_user_registration_path %>"><i class="icon-pencil"></i> Editar</a></li>
              <li class="divider"></li>
              <li><a href="<%= destroy_user_session_path %>" data-method="delete"><i class="icon-off"></i> Sair</a></li>
            </ul>
          </div>
        </div>
      </div>

      <hr>

      <div class="row">
        <div class="span4">
          <div class="well">
            <ul class="nav nav-list">
              <li class="nav-header">Trabalhos</li>
              <li><%= link_to "Mais Recentes", root_path %></li>
              <li class="divider"></li>
              <li class="nav-header">Por Instituições</li>
              <% College.all.each do |c| %>
              <li><%= link_to c.short, college_path(c) %></li>
              <% end -%>
            </ul>
          </div>
        </div>
        <!-- dynamic content -->
        <div class="span8">
          <%= yield %>
        </div>
      </div> <!-- /row -->

    </div> <!-- /container -->

    <!-- Javascripts
    ================================================== -->
    <!-- Placed at the end of the document so the pages load faster -->
    <%= javascript_include_tag "application" %>

  </body>
</html>
